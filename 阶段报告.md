# 视频转笔记工具 · 阶段报告

> 报告日期：2026-08-18
> 项目路径：`product/video-note/`

## 一、项目概述

一个「B 站视频 → 结构化总结笔记」工具：粘贴 B 站视频链接，自动下载字幕（或转写音频），再由大模型分块摘要、汇总生成 Markdown 笔记。

从最初的本地命令行脚本，逐步演进为带 Gradio 界面的本地工具，最终部署到阿里云服务器，供手机/电脑等多设备通过浏览器访问。

## 二、完成的功能

1. **链接解析**：支持 B 站完整链接、短链（b23.tv）、纯 BV 号、以及「【标题】+ 链接」的分享文本格式。
2. **视频信息与分 P 识别**：调用 B 站 API 获取标题、UP 主、时长、分 P 列表。
3. **分 P 选择**：解析后列出所有分 P（含标题、时长），可勾选要转换的（默认全选）。
4. **字幕优先**：用 yt-dlp 下载 AI 字幕（`ai-zh`），命中则跳过转写，又快又准。
5. **无字幕回退**：下载音频 → 云端转写（优先）→ 本地 Whisper 转写（兜底）。
6. **分块摘要（map-reduce）**：转录文本按 3500 字分块，逐块摘要，再汇总成一篇 Markdown 笔记。
7. **自定义输出标签**：内置「金融分析（默认）/ 学术分析 / 总结报告」三个模板，用户可新增、编辑、删除，标签即输出排版提示词。
8. **实时进度**：解析 → 字幕 → 下载 → 转写 → 摘要 → 汇总 全程显示进度。
9. **停止转换**：每个运行中的任务独立「停止」按钮。
10. **转换任务进度表**：历史 + 当前任务列表，带状态徽章、时间、进度消息、下载按钮、展开预览。
11. **并发转换**：后台线程并发执行，最多 4 个任务同时跑。
12. **登录鉴权**：用户名/密码（环境变量）保护。
13. **Cookie 方案**：字幕/音频下载按「cookies 文件 > 本地浏览器 > 无」三级回退。
14. **北京时间显示**：任务时间统一按 UTC+8 展示。

## 三、技术选型

### 前端
- **Gradio 6.24.0**：`gr.Blocks` 构建界面，`gr.Timer` + `gr.render` 实现任务表动态轮询渲染，`gr.DownloadButton` 下载文件，内置登录鉴权（`auth`）与文件访问白名单（`allowed_paths`）。

### 后端（Python 3.13）
| 组件 | 选型 | 用途 |
|---|---|---|
| 链接/信息获取 | `requests` 2.34.2 | 解析短链重定向、调用 B 站 API |
| 字幕/音频下载 | `yt-dlp` 2026.7.4 | 下载 AI 字幕、音频 |
| 云端语音转写 | 阿里云百炼 `qwen3-asr-flash` | 无字幕时云端转写（OpenAI 兼容接口 + base64 音频） |
| 本地语音转写 | `faster-whisper` 1.2.1（medium，CPU int8） | 云端不可用时的兜底 |
| 文本摘要 | DeepSeek `deepseek-v4-flash` | 分块摘要 + 汇总（`openai` 2.38.0 SDK） |

### 部署
- **Docker**：`python:3.13-slim` + `ffmpeg`，apt/pip 均走国内镜像。
- **docker-compose**：挂载 `./data` 卷持久化任务/笔记，环境变量注入 API Key、Cookie 路径、登录账号等。
- **服务器**：阿里云 ECS（114.215.191.203），对外端口 7860。

## 四、架构说明

核心逻辑（`video_to_note.py`）与界面（`app.py`）分离，前者同时支持 CLI 调用。

任务执行采用**后台线程 + 任务表轮询**的解耦架构：

- 前端点击「开始转换」→ `convert()` 启动后台线程并立即返回。
- 后台线程执行 `vn.run()`，进度实时写入 `tasks.json`。
- 前端 `gr.Timer` 每 5 秒轮询 `tasks.json`，渲染任务表。

这样**任务执行不依赖前端连接的存活性**——手机锁屏、刷新、断网都不会中断任务，回来即可从任务表看到结果。

## 五、遇到的问题集合

### 5.1 模型与 API

**1. 推理模型输出为空（摘要生成不出内容）**
- 现象：摘要/汇总阶段返回空内容，`finish_reason=length`。
- 根因：deepseek-v4-pro 是推理模型，`max_tokens` 被内部推理（reasoning tokens）耗尽，正文没额度。
- 解决：`max_tokens` 提到 8000。

**2. 摘要阶段太慢（一个分块 48 秒）**
- 现象：多分块任务动辄几分钟，体验上像「卡住」。
- 根因：`deepseek-v4-pro` 是推理模型，处理约 3900 字中文转录要 48 秒；而本项目只是文本总结，不需要推理。
- 解决：切换 `deepseek-v4-flash`（实测 13 秒/块，快 3.6 倍，质量相当）。

**3. 云端转写拿不到结果（DashScope Paraformer 下载失败）**
- 现象：转写接口返回 `oss://` 文件地址，后续下载报 `FILE_DOWNLOAD_FAILED`。
- 根因：异步文件型接口不适合当前链路。
- 解决：换成 `qwen3-asr-flash` 同步接口，音频转 base64 直接内联请求。

### 5.2 字幕与 Cookie

**4. 明明有字幕却判成「无字幕」**
- 现象：部分视频返回空字幕列表。
- 根因：B 站 AI 字幕接口需要登录态（Cookie）才返回。
- 解决：yt-dlp 增加 `--cookies-from-browser chrome`。

**5. 「400 Cookie Too Large」**
- 现象：带全量 Cookie 请求被拒。
- 根因：Cookie 体积超限。
- 解决：只发必要 Cookie（SESSDATA / bili_jct / buvid3 / DedeUserID）。

**6. 导出式 cookies 文件提示「not logged in」**
- 现象：`--cookies <文件>` 始终未登录。
- 根因：yt-dlp 导出的 cookies 格式不兼容。
- 解决：改用 `--cookies-from-browser` 直接读浏览器。

**7. 需登录字幕的视频漏掉**
- 现象：`need_login_subtitle=true` 的视频走不到字幕。
- 根因：此前部分用 B 站字幕接口，部分用 yt-dlp，逻辑分裂。
- 解决：字幕下载整体切换到 yt-dlp 一条链路。

### 5.3 网络与下载

**8. HuggingFace 模型下载超时**
- 现象：faster-whisper 模型下载卡死。
- 根因：HuggingFace 在国内不可直连。
- 解决：改从 ModelScope（modelscope.cn）下载。

**9. 数据中心 IP 下载音频被 412 拒绝**
- 现象：服务器下载音频返回 412 Precondition Failed。
- 根因：B 站风控识别数据中心 IP。
- 解决：下载音频时同样带上 Cookie。

**10. Docker 构建 apt-get 卡死**
- 现象：`apt-get update` 耗时 20+ 分钟。
- 根因：`deb.debian.org` 国内直连极慢。
- 解决：Dockerfile 里 sed 替换为 `mirrors.aliyun.com`。

### 5.4 Gradio / UI

**11. 主题参数警告**
- 现象：`theme` 传参报弃用警告。
- 根因：Gradio 6 中主题应传给 `launch()` 而非 `Blocks()`。
- 解决：移到 `launch(theme=...)`。

**12. 下载文件报 InvalidPathError**
- 现象：下载笔记时报路径非法。
- 根因：文件不在 Gradio 允许的路径内。
- 解决：`launch(allowed_paths=[OUTDIR])` 加入数据目录。

**13. 端口冲突**
- 现象：启动失败，7862 被占用。
- 根因：用户其他服务（LinguaMate）占用。
- 解决：改用 7860。

**14. 停止按钮位置不合理**
- 现象：停止按钮是独立卡片，不与任务对应。
- 根因：早期设计把停止做成全局控件。
- 解决：改为每个运行中的任务卡片内独立「停止」按钮。

**15. 手机端刷新后内容清空**
- 现象：浏览器刷新，正在转换的内容消失。
- 根因：任务进度只靠前端内存，未持久化。
- 解决：引入 `tasks.json` 持久化 + 定时轮询恢复。

### 5.5 并发与任务状态（本阶段重点难点）

**16. 任务卡在「转换中」不动**
- 现象：任务状态一直是「转换中」，永不结束。
- 根因：并发转换时，多个线程同时读写任务注册表产生竞态。
- 解决：`tasks.json` 读写加 `threading.Lock`，并对超时任务做「已中断」判定。

**17. 连续提交第二个任务无法并发**
- 现象：第二个任务要等第一个完成才进入队列。
- 根因：Gradio 事件默认 `trigger_mode="once"`（事件 pending 期间拒绝再次提交），之前只改了 `concurrency_limit`，没改 `trigger_mode`。
- 解决：`run_btn.click(..., trigger_mode="multiple")`。

**18. 任务卡在「摘要分析：分块 2/3」**
- 现象：任务永远停在某分块，状态还是「转换中」。
- 根因：刷新/关闭页面会触发 Gradio 关闭生成器、抛 `GeneratorExit`；而它是 `BaseException`，不被 `except Exception` 捕获，任务状态没被写回，变成僵尸「转换中」。
- 解决：补 `except GeneratorExit` 写回「已中断」；`_call_llm` 加 `timeout=300` + 重试，杜绝无限等待。

**19. 手机端报「页面连接已断开，转换中断」**
- 现象：正常等待转换却中断。
- 根因：任务执行原本是 SSE 流式生成器，与前端连接绑定；手机锁屏/切后台/网络切换/运营商 NAT 超时都会断开连接，进而中断任务。
- 解决：**架构解耦**——任务改到后台线程执行、只写 `tasks.json`，前端定时轮询展示，彻底脱离前端连接。

### 5.6 其他

**20. 任务时间与北京时间不一致**
- 现象：任务表时间慢 8 小时。
- 根因：`datetime.fromtimestamp()` 取容器本地时区，Docker 默认 UTC。
- 解决：显式用 `timezone(timedelta(hours=8))` 格式化。

**21. `_call_llm` 函数重复定义**
- 现象：代码合并后出现两个同名函数。
- 根因：多次编辑残留。
- 解决：删除重复定义。

## 六、后续可优化方向

- 分块大小可调（当前固定 3500 字，减小可进一步提速）。
- 任务表增加「清空已完成/已中断」按钮，避免历史堆积。
- 摘要/汇总阶段的进度进一步细化（当前按分块，而非 token 粒度）。
- 超时 running 任务自动写回 `interrupted` 状态（当前仅显示层判定）。

---

# 阶段二：商业化改造

> 报告日期：2026-08-21
> 阶段一交付后，从「个人工具」迈向「多用户商业产品」的改造。

## 一、阶段目标

把个人使用的单用户工具，改造成可收费的多用户产品。核心四件事：**成本核算、多用户改造、计费系统、支付与合规前置规划**。

## 二、完成的功能

1. **成本统计**：记录每次转换的 DeepSeek token 用量 + DashScope 转写时长，按峰谷计价计算成本（元），任务表展示 `💰 ¥x.xxxx`。
2. **多用户改造**：手机号 + 邀请码登录，PostgreSQL 数据库，任务/笔记文件按用户隔离。
3. **登录页 / 转换页分离**：未登录只显示登录表单，登录后切换转换页（`gr.render` 按登录态切换）。
4. **计费系统**：0.8 元/15 分钟向上取整；转换前余额预检查，完成后扣费写 `billing` 流水，失败/取消不扣费。
5. **充值功能**：页面充值入口（模拟充值，不接真实支付），悬浮弹窗内披露计费细则。
6. **二次确认转换**：点「开始转换」弹出确认窗（视频时长 / 分P / 费用），勾选分P实时更新预估费用，确认后才执行。
7. **解析分P集成**：移除独立「解析分P」按钮，开始转换自动解析并在弹窗内选分P。
8. **代码同步**：建立独立 GitHub 仓库 + SSH key，服务器部署源改为 GitHub（codeload 兜底）。

## 三、技术选型（阶段二新增）

| 组件 | 选型 | 用途 |
|---|---|---|
| 数据库 | PostgreSQL + SQLAlchemy（`db.py`） | users / tasks / billing 三表，替代 `tasks.json` |
| 登录 | 手机号 + 邀请码（环境变量 `INVITE_CODE`） | 内测零依赖，后续换短信验证码 |
| 会话 | Gradio `gr.State` 存 `user_id` | per-session 隔离 |
| 悬浮弹窗 | 自定义 CSS（`position: fixed` + 嵌套选择器） | 充值 / 二次确认弹窗 |

## 四、架构说明

**数据隔离**：任务按 `user_id` 查询/写入，笔记/音频/转写按 `data/{user_id}/` 分目录；标签 presets 暂保持全局。

**计费流程**：`convert` 解析时长算金额 → 余额不足拦截 → 后台线程转换 → 完成时扣费写 `billing`。`users.balance` + `billing` 表在改造时就预留。

**悬浮弹窗**：Gradio 6 无 `gr.Modal`，用 CSS 把弹窗 Group 做成 `position: fixed` 居中悬浮层，`max-height: 85vh` + `overflow-y: auto` 实现超出滚动。

## 五、决策记录

1. **商业化定位**：B2C 个人用户、按量计费、专注 B 站、小规模内测起步。
2. **定价**：0.8 元/15 分钟向上取整，不区分字幕。依据：市场价 1 元/15 分钟；实测成本有字幕 ≈0.0035 元/分钟、无字幕 ≈0.017 元/分钟，最坏毛利仍 68%，无需为省成本区分字幕。
3. **登录方式**：内测期手机号 + 邀请码（零外部依赖、快速跑通），正式期再接短信验证码。
4. **数据库**：PostgreSQL（用户指定），而非更轻的 SQLite。
5. **主体**：个人主体做收费有硬约束（个人备案不能经营、办不了经营性 ICP 许可），正式收费前注册个体工商户接微信/支付宝。
6. **部署源**：GitHub 独立仓库；国内服务器访问 GitHub 不稳，用 codeload tarball 兜底。

## 六、遇到的问题集合

### 6.1 Gradio / 前端

**1. gr.render 内 elem_classes 丢失（悬浮弹窗 CSS 失效）**
- 现象：给弹窗 Group 加 `elem_classes` + CSS `fixed` 定位，CSS 完全不生效，弹窗退化成普通展开。
- 根因：`gr.render` 内动态创建的组件，`elem_classes` 在前端渲染时丢失（对照实验：顶层组件正常、render 内组件丢失）。
- 解决：把弹窗 Group 移到 `gr.render` 外（顶层静态渲染）。

**2. 弹窗高度坍缩成 50px**
- 现象：弹窗宽度正常，高度却只有 50px，内容被裁切。
- 根因：`gr.Group` 渲染成嵌套两层 `gr-group`，`elem_classes` 同时应用到两层；`position: fixed` 误伤内层，内层 fixed 脱离外层文档流，外层高度坍缩成 padding+border（50px）。
- 解决：CSS 用 `.modal-box:not(.modal-box .modal-box)` 只作用最外层，内层 `.modal-box .modal-box` 恢复 `position: static` 撑开外层。用 Playwright 无头浏览器实测验证通过。

**3. css 参数警告**
- 现象：`css` 传给 `Blocks()` 报弃用警告。
- 根因：Gradio 6 中 `css` 应传给 `launch()`。
- 解决：移到 `launch(css=...)`。

### 6.2 部署 / 网络

**4. 服务器 git pull 超时（HTTP2 framing error）**
- 现象：`git fetch` 卡死报 `curl 16 Error in the HTTP2 framing layer`。
- 根因：国内访问 GitHub 的 HTTP2 协议问题。
- 解决：`git config --global http.version HTTP/1.1`，并改用浅克隆 `--depth 1`。

**5. codeload CDN 缓存旧代码**
- 现象：push 后 codeload 下载的 tarball 仍是旧版本。
- 根因：`codeload.github.com` 的 CDN 缓存有延迟。
- 解决：部署前先 grep 校验下载代码是否为最新，缓存刷新后重试。

**6. Dockerfile 漏 COPY db.py**
- 现象：容器报 `ModuleNotFoundError: No module named 'db'`，数据库表也没建。
- 根因：新增 `db.py` 后 Dockerfile 的 `COPY` 没加。
- 解决：`COPY` 补上 `db.py`（后续也补了 `recharge.py`）。

## 七、后续可优化方向

- 接入真实支付（微信/支付宝）替换模拟充值。
- 手机号登录接短信验证码，替换邀请码。
- 缓存去重：同一视频（按 BV 号）重复转换不重复调 API、不重复扣费。
- 内容安全审核接入。
- 合规落地：隐私政策、用户协议、ICP 备案、个体工商户注册。

---

# 阶段三：功能完善、运维监控与前端重写

> 报告日期：2026-08-23
> 阶段二交付后，完成商业化的收尾功能、补齐运维监控，并将前端从 Gradio 重写为原生前端。

## 一、阶段目标

1. **商业化收尾**：缓存去重（省成本）、短信验证码登录（替换邀请码）、支付订单系统（模拟支付）、合规文案（隐私政策/用户协议）。
2. **运维监控**：监控看板（业务/任务/成本/系统四层指标）+ 成本明细埋点。
3. **前端重写**：Gradio 无法还原 UI 稿视觉标准，改用 FastAPI + 原生前端重写（路线 B）。

## 二、完成的功能

1. **缓存去重**：同一视频（相同分P + 标签）重复转换直接复用已有笔记，不重复调 API、不重复扣费；进行中的相同任务拒绝重复提交。
2. **短信验证码登录**：手机号 + 短信验证码替换邀请码；阿里云短信 SDK（未配置时验证码打日志兜底）。
3. **支付订单系统**：`orders` 表 + 下单 + 幂等充值回调；前端「模拟支付」跑通，等商户号接真实支付。
4. **合规文案**：`docs/` 新增《用户协议》《隐私政策》，页脚展示。
5. **监控看板**：独立 7861 端口，业务/任务/成本/系统四层指标，Basic Auth 保护。
6. **成本明细埋点**：任务落库 input/output token、转写时长、失败原因归类（按最后执行阶段）。
7. **输出标签扩展**：默认标签改为 4 个（金融分析/学术速览/观点提取/故事梗概），支持自定义新增/编辑/删除。
8. **前端重写**：Gradio → FastAPI（`service.py` + `main.py`）+ 原生前端（`static/`），复用 UI 稿设计令牌，还原度 95%+。

## 三、技术选型（阶段三新增）

| 组件 | 选型 | 用途 |
|---|---|---|
| 后端框架 | FastAPI + uvicorn | REST API，替换 Gradio 作为服务端 |
| 前端 | 原生 HTML/CSS/JS + marked.js | 复用 UI 稿设计令牌，零构建 |
| 会话 | `sessions` 表 + Bearer token | 登录态（替代 Gradio `gr.State`）|
| 系统指标 | psutil | 看板读 CPU/内存/磁盘/负载 |
| markdown 渲染 | marked.js（本地打包） | 笔记预览渲染标题/加粗/列表 |
| 短信 | 阿里云短信 SDK | 发送验证码（未配置时 dev 兜底）|

## 四、架构说明

**缓存去重**：`tasks` 表加 `bvid`/`page_key`/`prompt_hash` 三列，已完成的任务即缓存。转换前先查「进行中」（拒绝）→「已完成」（复用，不扣费）→ 否则正常流程。

**会话鉴权**：登录返回随机 token 存 `sessions` 表（7 天有效），前端放 localStorage，请求带 `Authorization: Bearer <token>`；后端依赖解析 token → `user_id`。

**转换执行**：沿用后台线程 + 停止事件模型，前端每 5 秒轮询 `/api/tasks` 刷新任务表。

**前端架构**：FastAPI 服务静态文件（`static/`）+ REST API，7860 前端 / 7861 看板（Gradio 保留）。`service.py` 承载业务逻辑（与 UI 解耦），`main.py` 承载路由与鉴权。

## 五、决策记录

1. **缓存去重粒度**：per-user，key =（BV 号 + 分P选择 + 标签 prompt 哈希）。跨用户共享会泄露内容且破坏计费。
2. **支付路径**：个体户 → 微信/支付宝标准商户号，明确**不走二清/第四方平台**（2026 监管对二清零容忍，有千万级罚单与刑责风险）。
3. **前端路线**：先尝试 Gradio + CSS 优化（路线 A），实测还原度 15~20%（只能改配色，改不了布局/图标），达不到 UI 稿标准 → 走路线 B 重写。
4. **前端技术**：原生 HTML/CSS/JS（UI 稿本身是原生，零构建最省事），不用 React/Vue。
5. **短信**：阿里云短信（已有 ECS + 百炼，同一生态）；未配置时 dev 兜底打日志。

## 六、遇到的问题集合

### 6.1 前端 / Gradio

**1. Gradio 无法还原 UI 稿视觉标准**
- 现象：只改 CSS 后，与 UI 稿差距仍很大（自定义 SVG 图标、hero 渐变卡、标签 pill 等做不出来）。
- 根因：Gradio 组件渲染自己的 DOM，只能在外面套 CSS，无法把 SVG 图标塞进按钮、无法精确控制布局。
- 解决：改走路线 B，FastAPI + 原生前端重写，前端直接复用 UI 稿 HTML。

**2. gr.Group 嵌套渲染（卡片双重边框）**
- 现象：`elem_classes` 加在 `gr.Group` 上，类被同时应用到外层和内层，任务卡渲染出双重边框/内边距。
- 根因：Gradio `gr.Group` 渲染成嵌套两层，`elem_classes` 两层都生效。
- 解决：CSS 用 `.task-card:not(.task-card .task-card)` 只作用最外层（同弹窗问题的解法）。

**3. 任务表轮询重渲染导致预览滚动重置**
- 现象：展开预览后滑动，2~3 秒后自动滑回顶部。
- 根因：任务表每 5 秒 `innerHTML` 整体重渲染，把预览框和滚动位置重置。
- 解决：加 `tasksEqual()` 对比，数据没变化就跳过重渲染（只更新余额数字）。

**4. markdown 预览丢失格式**
- 现象：预览的文章没有加粗/标题层级，纯文本。
- 根因：预览用 `white-space: pre-wrap` 纯文本渲染，没渲染 markdown。
- 解决：引入 marked.js（本地打包，不依赖 CDN），预览用 `marked.parse()` 渲染，补 markdown 样式。

**5. 验证码日志缓冲（Docker 非 TTY）**
- 现象：点「发送验证码」后，日志里看不到验证码。
- 根因：Python 在 Docker 非 TTY 下 stdout 块缓冲，`print` 不刷新。
- 解决：`print(..., flush=True)`；重写后 uvicorn 作为主进程，日志直接进 `docker logs`。

### 6.2 部署 / 网络

**6. 服务器 git fetch 超时**
- 现象：`git fetch` 卡死（curl 16 HTTP2 framing error）。
- 根因：国内服务器访问 GitHub 不稳。
- 解决：改用 **scp 直接传文件**到服务器（绕开 GitHub），再 `docker compose up -d --build`。

**7. 测试端口未发布到宿主机**
- 现象：SSH 隧道连不上容器内的 uvicorn 测试端口。
- 根因：uvicorn 跑在容器内 7862，但该端口没映射到宿主机。
- 解决：docker-compose 临时加 `127.0.0.1:7862:7862` 绑定，测试完移除。

### 6.3 数据

**8. presets.json 覆盖代码默认标签**
- 现象：前端显示只有 3 个标签，缺「学术速览」。
- 根因：服务器残留 `data/presets.json`（测试时误写），覆盖代码里 4 个默认标签。
- 解决：删除残留 presets.json，让 `load_presets()` 回退到代码默认值。

## 七、后续可优化方向

- 接入真实支付（微信/支付宝商户号），替换模拟支付。
- 接入阿里云短信（真实短信），替换 dev 兜底。
- 前端 markdown 渲染安全加固（DOMPurify 防 XSS）。
- 会话 token 定期清理（过期 session 表会累积）。
- 音频/转写文件清理机制（data 目录长期累积）。
- 输出标签「设为默认」per-user（当前是全局默认）。
- 桌面/移动双端更精细的响应式断点。

