# 视频转笔记 · UI 稿迭代清单（终版）

> 交给 AI 据此迭代 HTML 稿。已合并全部已确认决策。

## 一、删除

1. 桌面端导航：删「历史笔记」「使用指南」，只保留「转换工作台」。
2. 桌面端统计卡：删「总任务数 / 成功率 / 本月消费」一行（统计由管理看板负责，用户端不展示）。
3. 失败任务卡：删「重试」「查看日志」按钮。
4. 页脚：删「Powered by Gradio v1.4.2」，换成自己的版权行（如「© 2025 视频转笔记」）。

## 二、文案修改

1. 桌面登录页「没有账号？联系客服获取」→「首次登录自动注册」。
2. 充值弹窗「失败任务自动原路退还，0 手续费」→「失败/取消不扣费，0 手续费」。

## 三、充值弹窗

- 保留「计费规则 + 金额选择 + 汇总」结构；支付方式区改为「模拟支付」占位（或保留微信/支付宝但标注「内测中，暂为模拟支付」）。

## 四、保留不改（代码后续实现）

1. 已停止任务卡的「继续转换」「删除记录」按钮 —— 保留。
2. 标签卡片的「设为默认」按钮 —— 保留。

## 五、补充缺失状态

1. 任务列表空状态（「暂无转换任务」）。
2. 二次确认弹窗「余额不足」状态。
3. 退出登录入口明确化：移动端 icon 加文字；桌面端头像下拉加「退出登录」项。
4. 转换中任务卡展示详细阶段进度（解析→字幕→下载→转写→摘要→汇总）。

## 六、一致性 & 细节

1. 移动端「已停止」任务卡补「继续/删除」按钮，与桌面端对齐。
2. 「ep 188」→「P188」或「第 188 集」。
3. （可选）移动端 hero 渐变卡与桌面端普通卡风格统一。
4. （可选）桌面任务卡状态图标用 SVG 替代「… ✓ ✕ ■」字符。

## 代码侧（我来改，不进 UI 稿）

1. 输出标签：代码从 3 个（金融分析/学术分析/总结报告）改为 4 个，对齐设计稿——金融分析（默认）/ 学术速览 / 观点提取 / 故事梗概，提示词对应设计稿描述。
2. 同步处理服务器 `data/presets.json`（旧 3 标签会覆盖代码默认值）。
3. （后续）实现「继续转换」「删除记录」「设为默认」功能。
